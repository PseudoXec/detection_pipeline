# config/secrets.yaml
# --------------------
# REAL credentials for this deployment. This file is gitignored and should
# stay chmod 600, owned only by the service account the pipeline runs as.
# Do not zip, screenshot, or paste this file anywhere - if you need to share
# your setup for support, share config/secrets.yaml.example instead.
#
# ACTION ITEM: the RTSP password below was sitting in plain text inside
# config.yaml, which just got zipped up and shared outside the Pi. Rotate
# this camera's password (in the camera's own web UI) and update the line
# below once you've done that - moving it into this file doesn't undo the
# fact that it was already exposed once.

camera:
  rtsp_url: "rtsp://admin:Victoria2313*@192.168.100.229:554/stream1"

live:
  # generated for you - random, 32 bytes, urlsafe-base64. Regenerate anytime
  # with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
  auth_token: "EIszWpHrTZYPznlgQ6X5uErEWm7UHFOdEtw83xh7utg"

api:
  endpoint_url: "http://192.168.100.14:5086/api/AiDetection/PlateDetected"
